# https://taskfile.dev

version: '3'

vars:
  COMMIT_MESSAGE: switched group order

tasks:
  default:
    cmds:
      - git add .
      - git commit -m "{{.COMMIT_MESSAGE}}"
      - git push origin master